# Skills

This folder contains M-LCA installable skills.

Create one folder per skill:

```text
skills/<skill-id>/SKILL.md
```

Keep this folder aligned with `marketplace.json`: every published entry should point to an existing `skills/<skill-id>/SKILL.md`.
